// M51MosaicClient.cpp - Implementation file (only contains implementations)
// All class definitions are in the header file

// #include "M51MosaicClient.h"
#include <QDebug>

// This file is intentionally minimal since implementations are in the header
// This avoids the duplicate definition errors

// If you prefer implementations in .cpp files, move the implementations 
// from M51MosaicClient.h to here and remove them from the header

void placeholder_to_avoid_empty_file() {
    // This function exists only to avoid compiler warnings about empty files
    // Remove this when adding actual implementations
}
